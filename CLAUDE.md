# חדר מצב — הוראות לעבודה על האתר

אתר חדשות אישי בעברית: https://ashbaruh.github.io/news-site/

## כללים
1. לכתוב רק בעברית.
2. הכול בחינם. אם אין פתרון חינמי — לומר את זה.
3. לבדוק כמו שבעל האתר פותח אותו: כקובץ (לחיצה כפולה על index.html), באתר עצמו, וגם בנייד.
4. לא לומר "עובד" לפני שהרצת את הבדיקות ופתחת את הדף.
5. אחרי בדיקה בדפדפן: להשתיק את המוזיקה ולסגור את הטאב.
6. לפני כל עבודה: `git pull` (בוטים בענן מעדכנים את האתר כל שעה).

## לוח זמנים (שעון ישראל)
| שעה | מה קורה |
|---|---|
| 02:40 | ניתוח מלחמות עמוק (Gemini) — פעם ביום |
| 03:40 | עדכון ביניים — מוכן ב-04:00 |
| **04:00** | **הכול מעודכן** |
| 05:40 | השלמה לניתוח העמוק (רק אם זירה נכשלה) |
| 11:40 | עדכון ביניים — מוכן ב-12:00 |
| 13:40 | השלמה נוספת לניתוח העמוק (אחרי חידוש המכסה של Gemini ב-10:00) |
| 17:40 | עדכון ביניים — מוכן ב-18:00 |
| כל שעה, דקה 17 | חדשות: AI, גלובס, ספורט, רכב, תוכן חיובי |
| כל הזמן | מחירים, מטבעות וקריפטו — חי בדפדפן |

- עדכון ביניים: עד 3 ידיעות לכל זירה (טלגרם ציבורי + מקורות זרים מהירים).
- את השעות מפעיל **cron-job.org** (חינם) — ראו `docs/SCHEDULER.md`. התזמון של GitHub הוא רק גיבוי.
- ריצה כפולה לא מזיקה: כל משימה בודקת אם כבר רצה.

## פרסום
- ניתוח שעובר את בדיקות האיכות עולה לבד.
- ניתוח חשוד מחכה לאישור: תגובה בהודעה של הבוט ב-GitHub:
  `מאשר` · `מאשר איראן` · `דוחה` · `בטל` · `עצור אוטומטי` · `הפעל אוטומטי`

## להריץ עכשיו (לשנות את הקובץ ולשמור ל-GitHub)
| מה | קובץ |
|---|---|
| ניתוח מלחמות | `.github/run-war-now.txt` |
| עדכון ביניים | `.github/run-brief-now.txt` |
| עדכון נתונים | `.github/run-data-now.txt` |

## בדיקות — לפני כל שמירה
```
python tools/test_war_contract.py
python tools/test_war_approval.py
python tools/mac/test_analyze.py
python tools/test_site_static.py
python tools/test_war_brief.py
```
אחרי שינוי ב-JS/CSS: `python tools/stamp_assets.py`

## אסור לשבור
- "אומת ב-N מקורות" מחושב בקוד, לא ע"י הבינה. טלגרם לבד ≠ מאומת.
- לא מעתיקים טקסט ממקורות — רק כותרת, קישור וניסוח מקורי.
- אין נתוני דמה. אין נתון — הקטע לא מוצג.
- מפתחות (Gemini, GitHub) רק ב-GitHub Secrets / cron-job.org — אף פעם לא בקוד.

פירוט נוסף: `docs/`.
