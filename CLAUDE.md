# חדר מצב — הוראות לעבודה על האתר

אתר חדשות אישי בעברית: https://ashbaruh.github.io/news-site/

## כללים קבועים
1. **לכתוב רק בעברית** — כל תשובה, הסבר ושאלה.
2. **הכול בחינם.** לא להציע שירות בתשלום. אם אין פתרון חינמי — לומר את זה בגלוי.
3. **לבדוק כמו שבעל האתר פותח אותו:** גם כקובץ (לחיצה כפולה על index.html) וגם באתר עצמו, ולא רק בשרת מקומי.
4. **לבדוק לפני שאומרים "עובד":** להריץ את הבדיקות ולפתוח את הדף.
5. **לא להשאיר מוזיקה מתנגנת:** אחרי בדיקה בדפדפן — להשתיק ולסגור את הטאב.

## לפני כל עבודה
- לעדכן מ-GitHub (`git pull`). בוטים בענן מוסיפים שינויים כל שעה.

## איך האתר מתעדכן (לבד, בענן)
- **כל שעה:** AI, גלובס, ספורט, רכב, תוכן חיובי (`update-data.yml`).
- **ניתוח מלחמות עמוק — פעם ביום** בבוקר (`war-daily.yml`, Gemini).
- **עדכון ביניים — 04:00, 12:00, 18:00**, עד 3 ידיעות לזירה (`war-brief.yml`).
- **פרסום אוטומטי:** ניתוח שעובר את בדיקות האיכות עולה לבד; חשוד — מחכה לאישור ב-GitHub.
- מחירים, מטבעות וקריפטו — נטענים בדפדפן בזמן אמת.

## פקודות ב-GitHub (תגובה בהודעה של הבוט)
`מאשר` · `מאשר איראן` · `דוחה` · `בטל` · `עצור אוטומטי` · `הפעל אוטומטי`

## להריץ עכשיו, בלי לחכות
- ניתוח מלחמות: לשנות את `.github/run-war-now.txt`
- עדכון ביניים: לשנות את `.github/run-brief-now.txt`
- עדכון נתונים: לשנות את `.github/run-data-now.txt`

## בדיקות (להריץ לפני כל שמירה)
```
python tools/test_war_contract.py
python tools/test_war_approval.py
python tools/mac/test_analyze.py
python tools/test_site_static.py
python tools/test_war_brief.py
```
אחרי שינוי בקבצי JS/CSS: `python tools/stamp_assets.py` (כדי שהדפדפן יטען את הגרסה החדשה).

## עקרונות שאסור לשבור
- רמת האימות ("אומת ב-N מקורות") מחושבת בקוד, לא ע"י הבינה. טלגרם לבד לא הופך ידיעה למאומתת.
- לא מעתיקים טקסט ממקורות — רק כותרת, קישור וניסוח מקורי.
- אין נתוני דמה באתר. אין נתון — הקטע לא מוצג.
- מפתחות (Gemini) רק ב-GitHub Secrets, אף פעם לא בקוד.

פירוט נוסף: תיקיית `docs/`.
