# שורות בפורמט אחיד (LF) — כדי שהמחשב המקומי (Windows) והמשימה בענן (Linux)
# לא ייצרו "שינויים" מדומים זה לזה
* text=auto eol=lf
# קבצי הפעלה של Windows חייבים שורות בפורמט של Windows, אחרת הם נשברים
*.bat text eol=crlf
